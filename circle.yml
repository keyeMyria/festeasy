machine:
  timezone:
    Etc/UTC
  python:
    version: 3.4.2

test:
  override:
    - nose2

deployment:
  staging:
    branch: master
    heroku:
      appname: fest-easy-backend
    commands:
      - npm install -g divshot-cli
      - cd www && divshot push staging --token $DIVSHOT_TOKEN