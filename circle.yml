machine:
  python:
    version: 3.4.2

dependencies:
  pre:
    - sudo apt-get -yqq install python3-dev
    - sudo ldconfig
  override:
    - pip install -qUr requirements.txt

test:
  override:
    - nose2

deployment:
  staging:
    branch: staging
    commands:
      - curl http://festeasy-staging.herokuapp.com:
          background: true
      - git push -f git@heroku.com:festeasy-staging.git $CIRCLE_SHA1:master
  production:
    branch: production
    commands:
      - curl http://festeasy-production.herokuapp.com:
          background: true
      - git push -f git@heroku.com:festeasy-production.git $CIRCLE_SHA1:master
      